# 完整微调后，选定粗块内部还留下多少细定位误差？

日期：2026-09-11。状态：实现、CPU测试与完整DINO历史组分析均已完成。分析从bf000e3执行，模型仍是原选中的epoch7，没有新forward或训练。

## 实际结论

验证集固定粗块的四子格oracle将PCK@8从81.44%提升到84.48%，额外救回53例；324个原@8错误中，仍有271例在选定块的四子格内不可达。它最多在该条件下补回3.04个百分点，不能填满相对HRNet的8.02个百分点差距。**单纯改善现有预测块内的子格选择不足以解释主要差距**；这不否定能够同时改变粗块选择的完整读出，也不证明应增加搜索半径。

## 为什么需要这项补充

[完整系统比较](2026-09-11-full-hrnet-dino-comparison.md)中，DINO/HRNet在共同1,746个有位置验证目标上的@8命中为1422/1562，@16为1537/1579。8到16像素误差区间的数量分别为115和17；两系统@8的140次命中差可算术分解成@16的42次差与区间内的98次差。这是误差分布分解，不是对分辨率、量化或motion的因果归因。

[搜索支撑分析](2026-09-11-search-support.md)又发现，精细位置差距主要发生在旧局部范围可覆盖的目标中。旧冻结小样本已做过[固定预测粗块的四子格oracle](2026-09-10-local-cost.md)，但完整数据和共同微调改变了表示与头，旧结果不能代替当前模型的读出判断。这一新证据支持补做同一种廉价诊断，不重开旧cost或宽度实验。

## 固定预测块的几何问题

当前DINO最终输出72×128，每格对应原图10像素；PixelShuffle×2的每个原生36×64粗块产生四个子格。对每一条实际预测，固定它选中的粗块，仅用GT在该块的四个合法输出中心中选择最近点。保持模型、检查点、实际预测、存在分数及阈值不变，oracle另作诊断统计。

全图最近输出格中心每轴至多距标签5像素，二维误差至多约7.07，因此@8在几何上可以全部命中。当前诊断测实际粗块与子格选择，不把10像素格距直接当成PCK@8不可突破的量化上限。

该oracle一定包含原预测点，因此其误差不能大于原误差。对每个有位置目标，@8分成互斥三类：实际已命中；实际未命中但选定块内存在8像素内子格；四个子格都不在8像素内。另报预测原生块与GT原生块相同的数量。

这三个指标不能混用：GT在相邻块也可能距当前块边缘子格不到8像素；同块则仍可能选错子格。因此不能用“是否等于GT粗块”直接代替PCK或oracle成功。

VC0没有合法位置，保持其实际坐标和存在分数，不为其构造oracle中心。VC1/2/3按现有位置协议分别报告；VC3的几何结果仍不意味着该帧有可见球外观。

## 范围与验证

只读已完成的DINO历史组config、results和train/val预测；不读图像、checkpoint或最终测试，不执行模型forward。复用原坐标映射和评价函数。输入格点检查用于防止把其他分辨率的预测误套成四子格；原PCK复现与oracle不劣关系用于发现坐标、块映射或结果文件错配。

汇总完整train/val、visibility和clip，报告实际与oracle的PCK8/16/32、上述三类数量及同GT块数量。若额外报告检测，则存在分数和阈值固定，明确这是使用GT的定位oracle结果，不能与真实模型成绩混为一列。

小型测试包含保持原命中、块内可恢复、跨GT块边界仍可达、四子格均不可达，以及VC0坐标不变；不重新执行训练测试或全仓检查。

实现见[分析脚本](../../scripts/analyze_tennis_subcell.py)，复用原坐标转换及评价函数；[一项综合几何测试](../../tests/test_subcell_diagnostic.py)已通过。以下完整分析已正常退出，[结果](../../outputs/full_heatmap/dino_prefix_seed0/subcell_diagnostic.json)单独保存，不覆盖模型预测。train/val的全体、visibility及clip PCK均重现；预测格点、oracle误差不增与VC0坐标保持均通过。

```bash
python -m unittest discover -s tests -p 'test_subcell_diagnostic.py'
python scripts/analyze_tennis_subcell.py --run outputs/full_heatmap/dino_prefix_seed0
```

## 完整运行的分解

| 集合 | 有位置n | 实际@8命中 | 固定块内可额外恢复 | 四子格均不可达@8 | 实际／oracle PCK@8 |
|---|---:|---:|---:|---:|---:|
| train全部 | 11762 | 11628 | 59 | 75 | 98.86%／99.36% |
| val全部 | 1746 | 1422 | 53 | 271 | 81.44%／84.48% |
| val VC1 | 1628 | 1384 | 47 | 197 | 85.01%／87.90% |
| val VC2 | 83 | 32 | 4 | 47 | 38.55%／43.37% |
| val VC3 | 35 | 6 | 2 | 27 | 17.14%／22.86% |

验证三个容差的命中数分别为@8的1422→1475、@16的1537→1544、@32的1568→1571。271个@8块内不可达目标中，69个最近子格位于(8,16]像素，27个位于(16,32]，175个超过32像素。因此“当前块内不可达”也包含不同程度的位置偏差，不能全部称为很远的背景误选。

同GT原生块的验证预测为1264/1746，反而少于1422个@8实际命中，直接说明两者不是同一指标；相邻原生块也能提供容差内位置。train同GT原生块为11519/11762。这些训练/验证差异描述当前模型，不足以分解成特征、监督或场景泛化的单一原因。

53个块内可恢复验证目标按Clip1–9依次为3、2、1、31、6、0、6、1、3；31个来自Clip4。其余来源与所有分组保存在结果文件中，不将相邻帧当作独立场景。VC2/3总共只多恢复6例，无法用简单子格选择解释这两个群体的多数错误。

固定原q与阈值时，@16检测TP/FP/FN从1525/265/221变为1532/258/214，F1由86.26%变为86.65%。这是GT oracle的诊断成绩；VC0的97个误报和原存在判断全部不变，不能将其写成可部署性能。

## 对下一步的影响

当前块内可恢复的53例占324个@8错误的16.36%；它是一个真实但有限的几何空间，**不证明现有特征包含足够信息让模型学会该选择**。即使全部实现，仍比HRNet实际@8少87次命中。当前不将仅增加PixelShuffle子格或调整块内选择列为主要修复；首先要保持区域选择、证据利用与背景竞争这一更完整的问题。

该oracle只是固定实际粗块之后的几何上限，不是所有新head、连续坐标或高分辨率结构的性能上限。当前粗块由所有细格logit的全局argmax间接选出；改变子格打分也可能改变粗块选择，两者在真实模型内并非独立决策阶段。

保持正在训练的真实历史/重复当前帧主要对照及已准备的集中度诊断；不据oracle直接增加新分支、监督或motion模块。后续具体改动还必须由可学习证据与公平对照支持。本项完整模型的固定块诊断到此结束，下一判断依赖完整时序对照与既定真实读出诊断。

# 全量训练的DINO系统能否超出稀疏探针的能力边界？

日期：2026-09-10；结果更新：2026-09-11。状态：正式seed0完成30epoch，最佳检查点、完整预测与指标核对通过。系统配对见[完整比较](2026-09-11-full-hrnet-dino-comparison.md)。
协议：[Tennis全量因果定位v1](../protocols/tennis-full-causal-v1.md)。设计：[全量强基线计划](../research/2026-09-10-full-baseline-plan.md)。

## 问题与锁定条件

与[全量HRNet](2026-09-10-full-hrnet.md)共享全量真实三帧、末帧目标、数据划分、512×288 RGB、标签语义和检测F1选优。现代系统从官方DINOv3 ConvNeXt-Tiny前两个stage与全新随机hidden32三帧SpatialProbe开始共同训练，避免继承旧step8实验的验证选优。它检验一个完整训练的现代竞争系统，不是新motion模块。

前缀输出192×36×64，三帧拼接576通道，输出72×128位置格和一个absence类别。沿用已验证的BackboneProbe和SpatialProbe，交叉熵监督末帧；位置取非absence格的argmax，存在分数为1−P(absence)，阈值0.5。seed0、batch8、float32、30epoch、AdamW weight_decay=.01，head lr3e−4/prefix lr1e−5均保持常数，无AMP或增强。

官方权重保留原名于models/pretrained/dinov3/lvd1689m/dinov3_convnext_tiny_pretrain_lvd1689m-21b726bb.pth。严格加载完整作者架构后只保留使用的前缀；未使用的后两stage释放。前缀eval保持当前构造行为，同时仍可计算梯度。模型移动GPU后再构造优化器，两个参数组单独记录。

本轮不是old/new×motion的2×2。相同目标和30epoch之外，两系统的输出尺度、监督、初始化和优化器仍不同，只能比较这两个具体系统。超参是运行前固定的实际条件，不声称各自最优。

## 实现与已完成验证

复用scripts/train_tennis_heatmap.py，新增实际使用的--model dino选项；默认hrnet路径保持原输入、QFL、Adam与epoch10/20衰减。DINO使用uint8 B×T×3×H×W传入BackboneProbe，在其内部做一次ImageNet归一化；HRNet先归一化，再合并为9通道。训练、评价、F1选优、保存checkpoint与逐帧预测共用既有循环，不创建新trainer或factory。

tests/test_full_model_inputs.py最初的一项CPU测试检查已知三帧像素、顺序和通道归一化，并用已知logits分别验证热图sigmoid及位置/absence softmax解码。测试先因新参数尚不存在失败，再通过；根代理复跑1项通过。独立审阅确认时间、归一化、初始化、梯度参数组、目标、损失和F1规则符合协议，无剩余阻断；语法编译和差异格式检查通过。

全量验证的相邻窗口反复包含相同帧，因此在DINO predict的每个batch内按真实RGB索引去重执行prefix，再恢复原窗口的时间与批次顺序。BackboneProbe.encode只抽出已有逐帧归一化与前缀计算，训练forward仍完整执行。复用范围限当前验证batch，不保存会随微调失效的特征。CPU测试扩为两项：重叠且顺序不平凡的两个三帧窗口，对照完整forward得到相同坐标与存在分数，prefix输入由6帧降为4帧；原前缀冻结/微调梯度测试也通过。实现代理记录先因重复帧数不符合预期而失败，再修改通过，根代理已阅读最终差异。

不同CUDA batch形状可能引入数值差异。队列已在HRNet结束后，用已有finetune_seed0的固定best.pt比较两条验证路径，覆盖全量game7的1,863个目标。要求1,746个有位置目标的预测坐标不变、所有目标的0.5存在判断不变，且存在分数在rtol=atol=1e−5内一致。实际全部1,863个坐标变化0、存在判断变化0、最大存在分数差0，通过预定条件。检查使用float32、batch8、固定epoch5与代码327879b，详见[dino_eval_reuse_gate.json](../../outputs/full_heatmap/dino_eval_reuse_gate.json)。这证明该检查点和输入上的复用一致性，不是所有将来模型的数值等价定理。

完整窗口与复用路径分别用时17.49/6.83秒；它们是固定先后顺序的单次执行，第一条包含首次运行条件，不据此报告稳定加速比。没有改变训练forward，也没有缓存随微调失效的特征。

```bash
python -m unittest discover -s tests -p 'test_full_model_inputs.py' -v
```

上述CPU检查只证明输入和读出逻辑；新入口的真实GPU训练与产物保存由下面单独执行的smoke验证，不以旧适配实验代替。

## 已完成的smoke与全量实验

HRNet正式运行使用32d37d4并已完成。DINO的smoke与正式训练均从327879b启动，config记录作者代码版本、官方权重路径和实际参数量。继续使用Conda zshihyc，没有新增依赖。

下面两条命令已按顺序执行。smoke复用已有20帧的独立小缓存，验证一次训练、选优恢复和预测保存，通过后启动全量。

```bash
python scripts/train_tennis_heatmap.py --model dino --rgb-cache outputs/full_heatmap/smoke_rgb --output outputs/full_heatmap/dino_smoke_seed0 --batch-size 8 --epochs 1 --seed 0
python scripts/train_tennis_heatmap.py --model dino --rgb-cache data/cache/tennis/rgb_512x288_all_h2 --output outputs/full_heatmap/dino_prefix_seed0 --batch-size 8 --epochs 30 --seed 0
```

smoke保存epoch0/1，单次训练loss=10.142424且有限，best_epoch=0，恢复后保存8/8行预测；总计1.504秒、峰值已分配5,260.87MiB。训练参数1,255,237，head/prefix学习率与协议一致。smoke仅检验链路，零定位分数和epoch0选优不构成模型能力结论。结果位于[dino_smoke_seed0/results.json](../../outputs/full_heatmap/dino_smoke_seed0/results.json)。

正式config记录12,167/1,863个目标、batch8、30epoch、float32及固定两个学习率，输出位于outputs/full_heatmap/dino_prefix_seed0。它在HRNet退出后启动，复用已有RGB，没有读取最终测试。30epoch已完成并恢复按预定规则选出的epoch7；下列结果来自该检查点的完整预测。

本地一次性顺序脚本outputs/full_heatmap/run_dino_after_hrnet.sh依次完成HRNet结果确认、CUDA帧复用检查、DINO smoke、正式训练和compare_predictions.py配对比较，正常退出。先前修改队列时已停止原等待进程并重新启动脚本，没有假设运行中的Bash会重新读取修改；原日志保留为dino_queue.initial.log，正式日志为dino_queue.log。

完整结果落盘后，复用HRNet已使用的核对逻辑，检查DINO的选优、目标身份、原标签和保存指标，再运行既定visibility上下文汇总。以下命令均已执行通过：

```bash
python outputs/full_heatmap/summarize_saved_result.py outputs/full_heatmap/dino_prefix_seed0
python outputs/full_heatmap/summarize_visibility_predictions.py
```

汇总另保存always_present_diagnostic：保持每帧预测坐标，将存在分数置为1，观察全部输出位置时的检测指标。它用于描述0.5存在拒绝对结果的影响，复用同一份预测，不选择新阈值。主结果仍采用预定0.5阈值；此诊断不能单独证明absence分支或运动表征是误差成因。

## 完整结果与判断

| 指标 | 训练 | 验证 |
|---|---:|---:|
| 有位置目标数 | 11762 | 1746 |
| PCK@8／16／32 | 98.86%／99.69%／99.78% | 81.44%／88.03%／89.81% |
| 位置误差中位数／均值，原图px | 3.81／4.33 | 4.95／39.77 |
| 检测F1@8／16／32 | 98.80%／99.59%／99.67% | 79.92%／86.26%／87.95% |
| 检测@16 TP／FP／FN | 11698／32／64 | 1525／265／221 |
| 存在判断TP／FP／FN | 11730／0／32 | 1693／97／53 |

全流程计时10,287.65秒（2.86小时），CUDA峰值已分配5,265.46MiB；包含取样、传输、在线训练、逐轮验证与最佳模型完整预测，不含缓存创建。最后epoch30结束时累计10,228.85秒，其后恢复与最终预测约58.80秒。不能把这个记录当作在线推理benchmark。

核对确认epoch0–30完整、30个训练loss有限、best.pt/results/预定F1选优均为epoch7；12,167/1,863行预测身份、顺序、原标签与缓存目标完全一致，全部保存指标复算一致。错误互斥分类与VC/clip总数通过。结果见[results.json](../../outputs/full_heatmap/dino_prefix_seed0/results.json)与[error_summary.json](../../outputs/full_heatmap/dino_prefix_seed0/error_summary.json)。

验证中1525个位置正确且输出，168个位置错误且输出，12个位置正确却拒绝，41个位置错误且拒绝；另有97个VC0误报。相对HRNet，更多检出同时伴随更多误检。整体训练拟合良好不代表跨game验证读出可靠，不能把差距直接归因为backbone或motion。

配对位置、visibility/历史、clip、固定12例和背景格点诊断统一见[完整系统比较](2026-09-11-full-hrnet-dino-comparison.md)。下一项为相同DINO条件下的重复当前帧重训，按[新对照协议](../protocols/tennis-full-temporal-control-v1.md)直接辨别真实历史增量。

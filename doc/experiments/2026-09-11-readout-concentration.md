# 高存在分数是否掩盖了分散的位置分布？

日期：2026-09-11。状态：有界探索，CPU数学测试与真实验证forward均已完成；固定统计已得到。

本项继承[Tennis全量因果协议](../protocols/tennis-full-causal-v1.md)的数据、标签与坐标规则，仅分析已选[DINO历史组](2026-09-10-full-dino.md)的epoch7检查点。它不训练新模型，不改动[重复当前帧对照](2026-09-11-full-temporal-control.md)，不作为独立benchmark。

## 为什么需要这项测量

[完整系统比较](2026-09-11-full-hrnet-dino-comparison.md)发现DINO的阈上有位置错位输出168例、VC0误报97例。现有CSV保存了存在分数与最终坐标，没有保存空间分布，因此尚不能区分：错误输出来自多个位置之间的分散响应，还是来自一个集中但错误的背景峰。

这会改变后续判断。如果错误同样高度集中，单纯给现有分布增加集中度读出就缺乏解释力；如果集中度含有额外区分信息，也应先承认这是现有读出的证据，再讨论新运动机制。不能仅因存在分数很高就称对应可靠，也不能预先把所有误报归给absence分支。

## 从现有实现可直接推导的事实

[SpatialProbe](../../src/ballmotion/probe.py)输出K=72×128个空间logits z，以及一个absence logit a。令

```text
p = softmax([z, a])          # K+1个类别的联合分布
q = 1 - p_absence            # 当前实现保存的“存在”分数
c = softmax(z)              # 只对K个空间logits归一化
v_hat = argmax(z)            # 当前实现输出的位置格
```

于是`p_v = q * c_v`。有位置目标格y的交叉熵可分解为`-log(q) - log(c_y)`，VC0的交叉熵为`-log(1-q)`。该分解不代表参数或优化独立：空间与absence读出共享前缀，q也依赖全部空间logits。

q汇总了所有空间位置的模型质量，而最终位置只选一个argmax。因此在同一个q下，单一位置峰与两个远隔峰可以有不同的局部集中度。反过来，一个集中但错误的峰也可以同时具有高q与高集中度。

归一化熵也不能按字面当可靠度：在9,216个格中只有两个等质量远隔峰时，熵为`log(2)/log(9216)`，归一化后数值较小，但选择其中哪个峰仍有歧义。本项比较实际分布和排序，不为“低熵”预设可靠标签。

这里“存在”始终指协议的有位置类别，包含VC3；不等于当前图像中存在可见球像素。上述代数关系不证明概率校准、物理对应或因果运动利用。若使用完整K+1的p，则邻域联合质量直接是`sum(p_v)`，不能再乘一次q。

## 固定测量与边界

只运行选定epoch7在同一1,863个验证目标上的一次forward，batch8、原精度和原模型输入。读取既有RGB缓存，不重新解码，不读取最终games8–10。正式运行等GPU训练结束后串行执行。

每个目标保存：q、`max(c)`、`H(c)/log(K)`、以预测argmax为中心的条件质量m16，以及`q*m16`。m16的邻域由原图格中心间距定义，半径16px；当前格距10px，所以它等于3×3九个格的概率之和，图像边缘只计有效格。邻域不依赖GT位置。

m16描述量化位置分布的集中度。格中心与真实连续坐标不同，加上硬格交叉熵未直接监督这个区域事件，不能把`q*m16`称为“16px内正确的已校准概率”。它只是本次要检验的固定统计量。

新forward的坐标必须逐目标重现已有预测，q需在rtol=atol=1e−5内相符；身份、顺序与原标签也必须相同。若不符，先定位实现或数值差异，不静默重排、丢行或将新预测混入旧错误分组。

通过复现核对后，报告中的q保留原CSV数值，联合质量也用原q与新测量的m16计算；这样两项排序比较共享同一份基准分数。乘法保持float32，与原模型分数精度一致。

## 在看结果之前确定的分析

沿用旧q≥0.5与16px误差，将样本分为：正确且输出、有位置但错位输出、位置正确却拒绝、错位且拒绝、VC0误报、VC0拒绝。分别报告各分数的p10、中位数与p90；不据结果重新定义组。

固定计分板格(54.5,664.5)的49例作为已有观察引出的事后组，连同是否输出单列。它只描述这个格点，不代替背景区域标注。按9个clip报告分数与排序方向，避免计分板重复或单个clip决定总体解释。

对事件“协议有位置且误差≤16px”，固定比较q与`q*m16`的AUROC，分别报告全体与旧阈上子集，并保留各clip结果。相等分数计0.5，无正例或负例时报告无定义。该排序统计不调新阈值、不重新选择检查点，也不替换已有检测F1或时序对照的判定规则。

支持空间分散解释需要看到：正确组较错位输出和VC0误报具有更高局部质量/峰值、更低熵，同时联合邻域质量的排序优于q。若错误组同样集中、固定背景错误具有高局部质量，或联合分数没有改善排序，就反对“仅靠空间集中度可以解释这些误报”的主张。两类错误可以表现不同，混合结果按条件表述；总体改善若由少数clip主导，结论相应收窄。

即使支持，也只说明现有logits存在未被q表达的信息，不证明新可靠性方法成立或必然提高F1。这是看结果前确定的解释边界，不据此新增head、loss、阈值搜索或组合训练；完整时序控制的结果另见[对应记录](2026-09-11-full-temporal-control.md)。

## 实施与实际结果

独立只读审阅核对了上述CE分解、联合质量、VC3语义与量化边界，支持该诊断与重复帧对照分别回答不同问题。[分析入口](../../scripts/analyze_tennis_readout.py)复用当前模型和预测读取，仅增加本项实际使用的统计，不改变训练入口。

[定位质量近邻审查](../literature/2026-09-11-localization-quality.md)进一步确认GFLv2、D-FINE作者实现与热图定位不确定性工作的直接先例。bbox边分布与中心位置分布不同，但“由集中度估计定位质量”已不能单独作为创新；本项仍只测量当前模型的具体失败。

[CPU数学测试](../../tests/test_readout_concentration.py)已运行通过：固定相同q的单峰与远隔双峰具有不同集中度，边缘邻域只含4个有效格，联合区域质量与直接切片求和一致，AUROC正确处理同分及严格排序。它只验证计算定义，不是模型诊断结果；主代理已审阅最终实现。

```bash
python tests/test_readout_concentration.py
```

重复当前帧训练正常退出后，已串行执行以下真实诊断；代码记录为c32a5af，模型仍为原历史组best.pt（epoch7）：

```bash
python scripts/analyze_tennis_readout.py --run outputs/full_heatmap/dino_prefix_seed0
```

输出为该run目录下的[readout_concentration.csv](../../outputs/full_heatmap/dino_prefix_seed0/readout_concentration.csv)和[readout_concentration.json](../../outputs/full_heatmap/dino_prefix_seed0/readout_concentration.json)。1,863行身份、顺序和标签一致，坐标逐行完全重现，q最大绝对差为0。耗时7.320秒，包含RGB缓存索引、GPU前缀/头forward、CPU统计及预测复现检查；没有重新解码、训练或读取最终测试比赛。该诊断耗时不作为完整检测系统的部署测速。

### 固定分组：分散响应确实存在，但错误也可能集中

下表为各分数中位数；完整p10/p90见JSON。q始终沿用原CSV，m16及其乘积按预定定义计算。

| 原分组 | n | q | max(c) | 归一化熵 | m16 | q×m16 |
|---|---:|---:|---:|---:|---:|---:|
| 位置正确且输出 | 1525 | 0.9999998 | 0.9660 | 0.0180 | 0.999994 | 0.999993 |
| 有位置但错位输出 | 168 | 0.9631 | 0.5762 | 0.1342 | 0.7106 | 0.6587 |
| 位置正确却拒绝 | 12 | 0.1751 | 0.6239 | 0.1628 | 0.8217 | 0.1639 |
| 错位且拒绝 | 41 | 0.0812 | 0.4080 | 0.2742 | 0.5085 | 0.0421 |
| VC0误报 | 97 | 0.9447 | 0.6724 | 0.1176 | 0.8647 | 0.8297 |
| VC0拒绝 | 20 | 0.0225 | 0.3036 | 0.3734 | 0.3542 | 0.0103 |

正确输出通常比错位输出和VC0误报更集中，符合预设的分散解释。但错误组的尾部不能忽略：错位输出与VC0误报的m16第90百分位仍为0.9815和0.9894。存在集中但错误的响应，因此集中度不是充分的正确性条件。

固定计分板格共有49次argmax，其中36次输出、13次拒绝，正确事件为0。该组q中位数0.5780、m16为0.4591、q×m16为0.2687，m16第90百分位也只有0.6070。它反复成为最高格点，却并非空间概率高度集中于这个邻域；“重复背景argmax”与“强而集中的错误峰”不能画等号。该组为此前观察形成的事后分析，不代表所有背景，也未据此选取阈值。

### 排序：总体改善，clip间并不一致

事件仍是“有位置且误差≤16px”，包含位置正确但被旧q拒绝的目标；总计1,537正例、326负例。全体AUROC从q的0.949908提高到q×m16的0.961197；旧q≥0.5的1,790个输出中，从0.946311提高到0.959504。

| clip | n | q的AUROC | q×m16的AUROC | 方向 |
|---|---:|---:|---:|---|
| 1 | 126 | 0.940586 | 0.961420 | 升 |
| 2 | 200 | 0.973333 | 0.984144 | 升 |
| 3 | 45 | 0.959459 | 0.949324 | 降 |
| 4 | 899 | 0.971460 | 0.981667 | 升 |
| 5 | 202 | 0.946628 | 0.934365 | 降 |
| 6 | 103 | 0.953785 | 0.959947 | 升 |
| 7 | 141 | 0.953416 | 0.958075 | 升 |
| 8 | 52 | 0.942708 | 0.937500 | 降 |
| 9 | 95 | 0.910282 | 0.870968 | 降 |

九个clip五升四降；Clip9降幅约0.0393。总体结果支持“原来的存在分数并未充分表达位置分布差异”这一有限解释，但不支持联合分数在各clip普遍更好。这里的分数为float32模型输出，q还可能饱和；没有把AUROC改善解释为校准质量、纯运动可靠性或某一种独立因果机制。

本项到此完成：没有改变位置、选择新阈值、回选检查点或报告新的F1。尤其q×m16≤q，在同一阈值下不能挽回此前被q拒绝的目标；且任何仅改变输出分数的方案都不会提高保持坐标不变时的PCK@8。后续若研究新的motion表示，仍需让表示/定位机制本身产生额外证据，不能用这次既有分布的排序改善代替。

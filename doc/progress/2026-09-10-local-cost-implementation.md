# 局部对应进入自动定位后的第一轮结果

日期：2026-09-10。长期研究继续推进，尚未形成最终算法或论文性能结论。

## 当前判断

[三组seed0对应对照](../experiments/2026-09-10-local-cost.md)已完成。centered cost在16px容差超过三帧stack与同参数self控制，但8px精度明显下降；raw没有净收益。这个结果允许进入预定seed复核，不能概括为定位全面提高或新颖motion机制成立。

已保存逐帧救回/新错、visibility、clip与搜索范围分组。范围外只有3/4个目标，不足以支持远搜索结论；困难和遮挡16px恢复仍为零。最大cosine在不少已有错误峰反而更高，不能直接当可靠性。固定预测native block的四子格诊断显示，精细退步大量发生在粗块不变时；下一结构判断要区分候选块与块内读出。

## 实施与证据

新增cost的缓存与读出经已知向量、时间/目标对齐测试、真实8目标集成smoke及独立只读审查。完整三个固定头在230个验证目标上，float32计算与float16存储cost未改变位置argmax或存在判断；该精度检查未解码任何图像。

三种完整cost共约2.37GiB，一次生成60.34秒，复用现有冻结特征。真实batch传输对照显示，先传float16再GPU转float32数值完全相同，局部中位耗时54.08→8.47ms；只改训练/预测两处表达式，不引入包装层或新依赖。整体训练耗时受共享资源和其他准备步骤影响，不使用该局部比值宣称整网加速。

[WASB实施证据](../literature/2026-09-10-wasb-implementation.md)已补齐：公开训练工程存在缺口，且原生VC3标签、MIMO监督与当前探针不同。可重建受控强基线，但不能混称为作者完整训练复现或直接充当严格motion归因。

## 接下来执行

1. 复用同一特征/cost，完成centered和self_centered各seed1、2；与已有stack同seed比较，继续保留PCK8退步和困难条件。
2. 根据多seed方向决定继续对应融合研究还是保留更简单的三帧路线，不先添加新模块。当前子格与峰值诊断已足够，不机械继续堆诊断。
3. 后续仍需强的完整训练基线与不同比赛/球种。Shuttlecock已有按rally的视频、CSV与异常记录可复用；尚无本项目开发split和位移统计。若进入该阶段，先固定Professional/Amateur的开发边界，保留Test，未标注尾帧不作为负例。

代码与文档持续提交；原始数据、权重及有复用价值的缓存保留。环境统一为Conda `zshihyc`，本阶段没有安装新依赖。

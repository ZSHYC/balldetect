<!-- AUTONOMY DIRECTIVE — DO NOT REMOVE -->
YOU ARE AN AUTONOMOUS RESEARCH AND CODING COLLABORATOR. COMPLETE CLEARLY AUTHORIZED WORK WITHOUT ASKING FOR ROUTINE PERMISSION.
DO NOT STOP TO ASK "SHOULD I PROCEED?" ON OBVIOUS, REVERSIBLE NEXT STEPS.
IF BLOCKED, TRY A REASONABLE ALTERNATIVE. ASK ONLY WHEN REQUIRED INFORMATION OR AUTHORITY IS MISSING, OR THE ACTION IS DESTRUCTIVE.
USE CODEX NATIVE SUBAGENTS FOR BOUNDED, INDEPENDENT SUBTASKS WHEN THEY MATERIALLY IMPROVE QUALITY OR THROUGHPUT.
<!-- END AUTONOMY DIRECTIVE -->

# AGENTS.md

本文件定义本仓库的长期研究与工程约束，不维护下载进度、阶段 TODO、当前最优指标或一次性实验结论。最新用户指令优先于这里的默认约定；既有研究文档中的候选方案不是已经批准的架构。

## 项目目标

本项目是高速微小球运动表征与逐帧定位的长期论文项目，主要场景为网球、羽毛球和乒乓球。目标是形成有实证支持的研究贡献，争取 CCF B 类及以上会议水平；不能承诺录用，也不能用工程完成度代替研究价值。

核心问题是：在球只有几像素、存在运动模糊、帧间位移可能远大于球尺寸的条件下，如何利用现代预训练视觉骨干，在有限计算量下保留并利用有效运动证据，提高逐帧球定位能力。

现代化 backbone 是设计前提，论文贡献应来自 motion representation 的机制与证据。不能把“TrackNet 换成 DINOv3”本身当作主要创新，也不能先堆 motion module、Transformer、attention 和 loss，再补研究动机。

## 任务边界

- 输入窗口只来自同一个连续 clip/rally；相机可以连续摇摄、跟随、缩放或改变视角。
- 第一阶段不研究切镜检测、跨镜头 tracking、跨镜轨迹 ID、SLAM 或三维重建，不默认引入重型相机运动估计网络。
- 模型窗口、时序缓存、轨迹先验和状态必须在片段边界重置。一个完整视频文件不自动等于一个连续镜头；边界不明确时不得把整场视频直接作为合法连续窗口来源。
- 预测对象为数据协议定义的二维球位置，以及协议需要的存在、可见或可定位状态。事件检测、姿态估计等不自动纳入任务。
- 可以研究固定前瞻的离线定位，也可以研究因果在线定位，但必须明确输入时间范围、预测目标帧、帧间隔及等待延迟。不能用未来帧的方法冒充在线方法。
- 优先使用公开数据及已有球位置标注，不把重新人工密集标注作为默认前提。新增数据集、研究任务或大规模训练应服务于明确研究问题。

## 研究方式与文献审查

- 从可证伪问题出发：具体失败发生在哪里，现有方法为什么不能解决，什么实验会推翻当前解释。先完成与当前决策相关的文献和数据审查，再实现最小有区分力的实验。
- 分清来源事实、逻辑推论、待验证假设和本地实测。实验未运行就写未运行，负结果也记录；不虚构指标、速度、资源预算或创新结论。
- 新颖性判断要对照最接近的先例，包括最新可访问的 arXiv 论文、正式论文和作者代码。记录论文标题、版本或日期、直接来源，以及与本项目的实质区别；搜索未命中不等于不存在，不能声称检索“万无一失”。
- 球定位相关审查至少覆盖 TrackNet 至 TrackNetV6、WASB、TTNet、TOTNet、BlurBall、RacketVision 及相关体育小球方法。不能再笼统声称 TrackNet 没有 motion；逐版本核对实际机制与实验协议。
- 对应搜索、时序累积、光流、点跟踪及视频预训练路线，要检查各自近邻工作。检索范围包括 MotionSqueeze、SELFY/STSS、TDN、MotionFormer、RAFT/FlowFormer/WAFT、CoTracker3、TAPNext，以及研究文档中列出的近期方法。
- 微小高速目标的创新审查应覆盖体育之外的相关先例，例如红外、无人机与卫星场景；OTHR/FlyingTO、MOCID、DQAligner、MIST、DeepPro、DMR、CMRTrack 等按与候选设计的关联核对。
- 不轻率宣称“首次小目标 motion”“首次大位移小目标”“首次全局/局部运动解耦”或“首次运动可靠性判断”。对每个准备写入论文的贡献，给出最近邻、差异和支持该差异的实验。
- 对抗性审查用于改变研究决策，不用于强行找问题。已经有证据支持的判断直接接受；只有新证据、方法变化或未解决的实质疑问才重开审查。

## 运动概念与模型设计

- 帧差表示变化，不等同于真实位移；correlation/correspondence 表示匹配支持，不自动等于可靠物理对应；全图光流指标不能代替球区域质量。
- 轨迹历史和运动预测是先验，不能重新包装为当前帧的直接视觉证据。单帧拖影反映曝光期间运动，不等于帧间位移；拖影方向轴也不自动带有速度正负方向。
- 优先定位信息损失发生在哪一步：resize、patch/stride、下采样、候选筛选、对应搜索、背景竞争或时间融合。冻结线性探针失败不证明所有可用信息都已消失。
- 比较现代 backbone 的浅、中、深层及空间尺度。DINOv3 ConvNeXt-Tiny/Small 与 ViT-S/16 是优先考察路线，实际采用前核对官方可用架构与权重；不得假设更新、更大就更适合微小球定位。
- dense adapter、高分辨率细节分支和 V-JEPA 2.1 等视频特征探针按实验证据引入，不预建全部路线，不默认使用巨型视频基础模型作为主干。
- 重点考察“微小空间支撑、大位移搜索、计算效率”的矛盾，但不预设显式 cost volume 必胜。简单帧堆叠、差分、时序累积、检测后关联和轨迹先验都可以成为有效竞争解释。
- 候选阶段的漏球不能被下游模块的条件成功率掩盖。扩大搜索范围既可能提高覆盖，也可能增加背景误匹配；需要分别测量。
- 视频 MLLM 能理解 motion，不等于能精确定位几像素球。给定 GT 初始化的点跟踪，也不等于自动发现和逐帧球检测。
- 在没有可靠对应时，不能把强行选出的单个位移当作已验证运动证据；采用多假设、拒绝匹配或可靠性建模前，同样审查先例与必要性。

## 数据与时间语义

数据入口和发布异常见 [data/README.md](data/README.md) 及各数据集目录说明。原始视频、图片、标注和发布版本保持不变；修正标签、派生索引、可视化与预测结果另存。

- TrackNet Tennis 用于网球主实验与公平比较；Shuttlecock 用于羽毛球及跨球种验证；BlurBall 用于模糊条件研究；OpenTTGames 用于高帧率视频与稀疏标签场景。后续扩展数据集应有明确验证目的。
- 必须保留原始帧号、时间戳、clip/rally、来源比赛、坐标系和原始 visibility/status。统一表示不能覆盖掉数据源的语义。
- **未标注不等于没有球。** OpenTTGames 未出现的 JSON key 是未知标签；有标签帧附近的真实连续帧可以作为 motion 输入，但不能把稀疏标签重新编号成连续视频。明确无球、未标注、不可定位、遮挡和非法坐标分别按源协议处理。
- OpenTTGames 的 `(-1,-1)` 与只有一边为 `-1` 的坐标不能混同；后者不能直接作为有效位置回归目标。其他数据集也不得共用未经核对的缺失值规则。
- 原始标注和 TrackNetV3 等修正版必须分别报告版本，不能静默混用。BlurBall 的中点、端点、角度与半长度标签需明确几何定义，不能互换监督目标。
- FPS、分辨率和视频时间从真实元信息读取；需要精确时间时使用逐帧 PTS。只有图片的发布包若没有原始 PTS，应注明论文报告或推导依据，不能捏造时间戳或曝光参数。
- resize、crop、padding、翻转和时间采样必须同步更新坐标与相关标注，并保持目标帧对齐。不能把插值位置、伪标签或 oracle 输入伪装成原始标注或正常推理输入。
- 带 GT 标记、轨迹覆盖、预测热图的可视化文件不得作为原始视觉输入。测试集标签不能进入训练、候选筛选、超参选择或缓存内容。
- 数据划分保留明确的比赛/视频/clip 归属。禁止随机打散相邻帧造成泄漏；game-level 与 clip-level 协议分开报告。复现采用可追溯的作者划分，缺失划分文件时不能猜一个比例后称为官方协议。
- 发现上游标签或目录异常时记录具体影响，不通过静默删行、裁剪坐标、改名或修正 split 让结果表面通过。实验确需处理时，保存处理规则及受影响样本，原件保留。
- 保存数据来源与使用条件；代码许可证不自动代表源视频许可。本地科研使用、论文展示和数据再分发按各自实际授权处理，不额外搭建许可审批框架。

## 实验归因与评价

- 先建立可复现、足够强的最小基线，再比较机制。至少规划 old/new backbone × no/new motion 的 2×2 归因实验，区分预训练、模型规模、空间分辨率和运动机制的贡献。
- 同一主要比较共享数据划分、标签版本、目标帧、输入帧数与时间间隔、前瞻范围、预处理和评价规则。无法保持一致的条件明确报告，不把不可比结果拼成排名。
- 明确冻结、微调及从头训练条件。论文代码、权重与本地复现结果分别注明来源；自动发现、GT 初始化、oracle 候选和真实候选分开报告。
- 主要评价逐帧位置误差及协议规定的定位/检测指标；记录匹配容差的单位、分辨率和可见性规则，不用逐帧 accuracy 掩盖漏检、误检或位置偏差。轨迹平滑后的结果与原始模型输出分开报告。
- 根据研究假设分析球尺寸、位移、模糊、遮挡、背景竞争和相机运动条件。`rho = frame displacement / ball size` 只在分子、分母有可信定义时使用；只有中心标签时不能虚构球尺寸，代理量必须说明依据。
- 阈值、容差相关超参和后处理在训练/验证数据上确定，不反复查看测试结果调参。跨球种适配与零样本泛化分开表述。
- 消融一次解决一个可区分的问题，保留必要对照；不机械排列全部模块组合。小规模 smoke 只证明运行通路，不能支撑性能或新颖性结论。
- 统计分析考虑比赛/clip 内帧的相关性；数千相邻帧不等于数千独立样本。按实际独立来源与随机性报告结果，不用复杂统计掩盖来源过少。
- 每次正式实验保存代码版本或明确改动、模型与预训练权重版本、数据/标签版本、划分、配置、随机种子、设备、实际命令和输出位置。用普通配置和记录即可，不为复现默认增加内容哈希或实验管理框架。

## 工程实现与计算效率

- 代码精简、可读，优先复用已有代码、Python 标准库和已安装依赖。改动小而聚焦，不顺手重构无关模块。
- 参数只为实际变化项服务。数据根目录、模型选择、输入尺度和时间采样可以配置；某个数据源特有的 schema 可以在对应读取代码中明确表达，不为假想格式预建 adapter/factory、兼容层或插件系统。
- 不复制参考仓库的 conda 环境名或硬件假设。使用本项目已明确的环境；确需新增依赖时说明用途并更新实际依赖记录，不能直接修改无关项目环境。
- 优先缩短从数据到可信结论的总周转时间：先删除无用工作，再减少重复计算，再考虑批量化；并发、编译、分布式及专门缓存只有在具体瓶颈成立时引入。
- 静态目录索引、CSV/JSON 与视频元数据集中读取，避免在每个样本或每个 epoch 重复扫描。视频读取考虑连续性和重叠窗口复用；不默认把全部视频展开成图片，也不把窗口复制保存成巨量重复数据。
- **冻结特征探针与端到端训练采用不同的数据路径。** 冻结且确定性的特征可按帧/clip 缓存；backbone 正在更新、输入增强变化或特征依赖时间上下文时，不能错误复用失效缓存。端到端训练允许必要的视频解码与 backbone forward，不照搬“训练阶段禁止解码/提特征”的限制。
- 缓存只保存确有复用价值的中间结果，用清晰目录和配置说明依赖；seed、模型头或后处理变化不应触发无关重算，影响输入或特征的变化也不能静默命中旧缓存。
- 不把高度重叠窗口持久化为重复数据；保留紧凑逐帧数据和时间索引，按需构造 batch。多窗口输出聚合要定义清楚，不能为节省计算改变原方法语义。
- 阈值或评价容差变化优先复用同一份预测，不重复模型 forward。微小目标定位涉及数值精度，混合精度或近似计算需确认不会改变关键位置结果。
- 性能报告说明硬件、输入尺寸、帧数、batch、精度和计时边界；区分解码/准备、backbone、motion、定位头及后处理成本。报告实际涉及的耗时、吞吐和显存，不能用缓存特征或忽略前瞻等待的速度冒充端到端在线速度。
- 长任务提供简洁的进度、阶段与错误信息。没有缓存就不添加 cache hit/miss 仪表；没有性能瓶颈证据就不创建优化框架。
- 本地数据、模型权重、缓存和训练产物默认不提交；临时脚本不要散落根目录。不得为整理目录移动原始数据或破坏已有实验引用。
- 外部预训练权重位于 `models/pretrained/<模型家族>/<预训练来源>/`，入口见 [models/README.md](models/README.md)。保留发布文件原名，不覆盖、不与本项目训练 checkpoint 混放；训练产物放入实际实验的 `outputs/` 目录。新增预训练来源时明确架构和归一化条件，不因名称近似静默替换。

## 文档与目录

新增项目说明、研究反馈和进展记录使用中文；论文草稿、代码标识符、原始论文标题及原样保存的上游文档按用途保留语言。

文档统一入口为 [doc/README.md](doc/README.md)。研究论证位于 `doc/research/`，文献证据位于 `doc/literature/`，实验协议位于 `doc/protocols/`，实验记录位于 `doc/experiments/`，阶段进展位于 `doc/progress/`；各类文件的命名和格式遵循文档导航。数据事实仍保存在 `data/README.md` 及其子目录，不复制维护第二份。阅读相关材料即可，不为一次局部修改重读全部文献。

按实际工作逐步使用 `src/`、`configs/`、`scripts/`、`tests/`、`outputs/` 等目录；只有需要时才创建，不预生成空工程骨架。新增阶段进展放在 `doc/progress/`，记录研究问题、采取的行动、实际结果与下一步判断。历史研究文档保留版本，整理路径时同步修正引用；不并行维护 `docs/` 或旧的 `research_notes/`。`AGENTS.md` 只保存长期约束。

## 验证与精简范围

这不是安全攻防论文。除非本项目另有说明，默认操作者是自己机器上的合作者。凡是受支持用法下确实会发生的问题都应报告；修法限于实际范围，不为了交差硬找问题。

- **不加哈希、校验和或指纹，除非它替代了一个实质上更贵的操作，并且结果会改变下一步。** 不默认生成文件指纹清单或反复扫描数据副本。
- 不为本项目不会发生的情况添加 feature flag、迁移框架、兼容层或包装层。
- 冷门编码、符号链接竞态、毫秒级竞态不在范围内，除非经由本项目受支持的用法可达；“理论上构造得出”不算。
- 该判断的地方就判断，不用评分表、检查清单代替判断，也不对已定论的东西再跑一遍。
- 以上不覆盖用户、本项目约定或更高优先级规则明确要求的安全、迁移、校验与审阅。

**跑任何检查前，先回答：会检测出什么具体失败？真出现了，我会做什么不同的事？答不上来就不跑。**

验证围绕本次改动选择最小有效范围。例如修改时间窗口时验证跨片段和稀疏帧对齐，修改坐标变换时用已知点验证映射，修改模型时用一个 batch 检查输出/梯度与目标帧对应，修改指标时用能区分漏检、误检和边界容差的小样例。只有相关逻辑变化才运行对应检查；纯文档修改不触发数据重验或训练。

先小规模确认研究与实现路径，再运行已授权的正式实验。发现失败就定位并修复；通过后没有新理由不扩大或重复验证。不声称未运行的检查已经通过，也不把下载完整性、模型可运行和论文结论成立混为一谈。

## 协作与交付

明确、低风险、可逆且已授权的下一步直接完成，不反复询问“是否继续”。涉及实质研究分支、缺少必要信息、破坏性操作或没有授权的对外发布时再澄清。用户中途更新约束时立即纳入当前工作。

独立文献检索、不同数据源审查等确有并行价值时可使用原生子代理，指定职责和文件范围；主代理整合结果并负责最终判断。不为微小修改启动审阅流水线，不递归扩大研究范围，不派多个代理重复检查同一已确认结论。

最终回复先说明结果，再给实际修改文件、必要验证证据和影响使用的未解决问题。研究任务交付研究判断与证据，代码任务交付可运行改动；不把计划、模块清单或一份评分表当作已经完成的结果。
